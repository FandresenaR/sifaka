{
  "name": "sifaka",
  "version": "0.2.0",
  "private": true,
  "packageManager": "npm@10.0.0",
  "description": "Sifaka CMS - TurboRepo Monorepo",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "latest"
  }
}
